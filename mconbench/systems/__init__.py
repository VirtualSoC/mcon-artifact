"""System drivers: thin wrappers over each stack's control tooling.

MCon is first-class; baseline drivers can be added later behind the same
interface (boot / warm / provision / stop) so experiments stay system-agnostic.
"""
